<div class="row">
  <div class="col-md-6">
    <h2> <%= @product.title %> </h2>
    <div style="height:100px;">
      <p>
        <%= @product.description %>
      </p>
    </div>
    <div> Quantity: <%= @product.quantity %> </div>
    <div class="product-price"> $ <%= @product.price %> </div>
    <div class="pull-right">
      <%= link_to("Buy", "#", :class => "btn btn-primary btn-lg btn-danger") %>
    </div>
  </div>
</div>